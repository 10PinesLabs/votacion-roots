<div class="text-extensible" {{action onClick}}>
  <p style={{if expandido descriptionMaxHeight}} class="descripcion-minuta {{if expandido "expandida" "colapsada"}}">
    {{markdown-view value=texto}}
  </p>
</div>